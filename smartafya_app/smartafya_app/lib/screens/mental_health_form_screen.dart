import 'package:flutter/material.dart';

import 'single_booking_screen.dart';

/// Entry for the single booking screen (includes description + consent + dates).
class MentalHealthFormScreen extends StatelessWidget {
  const MentalHealthFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SingleBookingScreen();
  }
}
